server:
  port: 8088
eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: true  #缓存信息
    register-with-eureka: false  #服务本身不用注册
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/


